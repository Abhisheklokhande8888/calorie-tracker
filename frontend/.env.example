# Leave empty when using Vite dev proxy to the API (see vite.config.js).
# For production or custom API host, set the backend base URL (no trailing slash):
# VITE_API_URL=http://localhost:5000

VITE_API_URL=
