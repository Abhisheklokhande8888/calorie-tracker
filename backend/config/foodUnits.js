/** Allowed unit keys for food entries (what “per unit” calories/protein refer to). */
export const FOOD_UNITS = ["100g", "100ml", "1g", "serving", "cup", "tbsp", "piece", "ml", "oz"];

/** Default unit everywhere (per 100g nutrition labels). */
export const DEFAULT_FOOD_UNIT = "100g";
